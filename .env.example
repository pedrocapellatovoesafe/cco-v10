HOST=localhost
NODE_ENV=development
PORT=3333
APP_URL=http://${HOST}:${PORT}

# URL da API (cco-api). Em dev pode ficar vazio (usa localhost:3333 por padrão).
# Em produção, defina como variável/secret no build do GitHub Actions.
VITE_API_URL=